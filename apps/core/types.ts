export type CoreConfig = {
  core: {
    supportEmail: string
    websiteUrl: string
    projectName: string
    darkMode?: boolean
    appTitle: string
    appDescription?: string
    appKeywords?: string[]
  }
}
